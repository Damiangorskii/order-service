server:
  port: 8085
  error:
    include-message: always

management:
  endpoints:
    web:
      exposure:
        include: prometheus
  endpoint:
    health:
      show-details: always

internal:
  api:
    shopping-service:
      url: http://localhost:8084/shopping/cart

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: order
      username: order-service
      password: Password123!

logging:
  level:
    root: INFO
    com.example.orderservice: DEBUG